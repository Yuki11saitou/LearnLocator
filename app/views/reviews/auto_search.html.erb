<% @spots.each do |spot| %>
    <li class="py-2 px-4 text-sm text-gray-800 bg-blue-100 hover:bg-blue-200 focus:bg-blue-200" role="option" data-autocomplete-value="<%= spot.id %>" data-autocomplete-label="<%= spot.name %>">
        <%= spot.name %>
    </li>
<% end %>